# This is the GUI for the drink mixer
from tkinter import Tk

class drink_mixer_gui(object):
    """
    This is what will take over the screen on startup.
    """
    def __init__(self):
        self.window = Tk.Tk()